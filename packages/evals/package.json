{
  "name": "@bluecairn/evals",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "test": "echo 'stub package — no tests'",
    "eval": "echo 'stub package — no eval suites'",
    "harness:m1": "bun run src/harness/m1-exit-gate.ts",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@bluecairn/core": "workspace:*",
    "@bluecairn/agents": "workspace:*",
    "postgres": "^3.4.5"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "typescript": "^5.7.2"
  }
}
