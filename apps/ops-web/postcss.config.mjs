/**
 * Tailwind CSS v4 uses a first-class PostCSS plugin — no separate
 * `tailwind.config.ts` file is required. All theme customization lives
 * inline in `src/app/globals.css` via the `@theme` at-rule.
 */
export default {
  plugins: {
    '@tailwindcss/postcss': {},
  },
}
